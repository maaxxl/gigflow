name: Deploy Flutter Web to Strato via SFTP

on:
  push:
    branches: [main]         # löst aus bei jedem Push auf main
  workflow_dispatch:         # erlaubt manuelles Auslösen über GitHub UI

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Code aus Repository holen
        uses: actions/checkout@v3

      - name: 🧰 Flutter installieren (neueste stabile Version)
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.16.9'
          channel: 'stable'

      - name: 📦 Pakete installieren
        run: flutter pub get
        working-directory: .

      - name: 🧪 Web-App bauen
        run: flutter build web
        working-directory: .

      - name: 📤 Hochladen via SFTP zu Strato
        uses: wlixcc/SFTP-Deploy-Action@v1.2.4
        with:
          username: ${{ secrets.root }}
          server: ${{ secrets.h2815428.stratoserver.net }}
          port: 22
          password: ${{ secrets.wokmi4-hekkib-sowqAd }}
          local_path: build/web/
          remote_path: /var/www/gigflow/                    # Zielverzeichnis auf Strato-Server
          sftp_only: true
